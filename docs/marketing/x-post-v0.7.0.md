thread, one post per block:

shipped Brainstorm v0.7.0. your inbox can trigger automations now — new email comes in, an AI step reads it and decides what to do. attachments are real files you can search and link. still all local.

the agent can also draft a reply and drop it into your composer. and semantic search only downloads its model if you actually turn it on.

the SDK's on npm now — @brainstorm-os, MIT — if you want to build on it. getbrainstorm.online

single-post version:

shipped Brainstorm v0.7.0 — your inbox can trigger automations now (new mail comes in, an AI step reads it and decides what to do), attachments are real files you can search and link, and the agent can draft replies into your composer. still all local. getbrainstorm.online
