- [x] Notes have all editor styles duplicated — **done 2026-07-17**: Notes now imports `@brainstorm/editor/editor-theme.css`; its ~2.2k-line private copy was deleted (294/296 overlapping selectors were byte-identical; the 2 drifted rules resolved explicitly). Shell PR #177.
- [x] Harness has duplicated shell files — **audited + fixed 2026-07-17**: `apps`/`packages`/`app`/`tests/soak`/`.size-limit.json` are symlinks into the shell repo (not copies); `tools/` and `tests/` trees are disjoint; the `docs/art` ↔ `packages/shell/art` image duplication is by design (`build:icons` source). The one genuinely stale duplicate — `docs/help-manifest.json`, superseded by the vendored `packages/shell/help-content/help-manifest.json` which every consumer (build-help-corpus.mjs, help-manifest-validator.test.ts) prefers — is removed in this commit.
- [ ] Move code shared between apps to organisation packages and each app to its own repo, so apps can have separate releases but use the same core functionality and components. This way we can easily add new apps as github repos + manifest and check their updates with new releases basically. This will also allow third party providers to be added as app manifest providers. — *not actioned 2026-07-17: multi-repo split is a large architecture project (per-app release/versioning infra, SDK publishing, manifest-provider protocol); needs a design doc + implementation-plan stage, not a session task.*
- [ ] change dnd in form editor to dnd kit with animation, verify other places — *not actioned 2026-07-17: real gap (form-designer uses native HTML5 dnd; `@dnd-kit` is already in the workspace via the database app), but it's a UX/animation change needing design + visual verification across apps — schedule as its own iteration.*
- [ ] add custom CSS per app and per object to tasks — *not actioned 2026-07-17: new capability surface (user style injection) — needs design + security review before implementation.*
