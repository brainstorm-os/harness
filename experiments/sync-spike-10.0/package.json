{
  "name": "sync-spike-10-0",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Throwaway de-risk spike for plan iteration 10.0. The proof lives in docs/_review/2026-05-20-10.0-sync-spike.md.",
  "scripts": {
    "predeps": "test -e node_modules || (test -d ../../packages/shell/node_modules && ln -s ../../packages/shell/node_modules node_modules) || (echo 'sync-spike-10.0: packages/shell/node_modules missing; run `bun install` at the repo root first' >&2 ; exit 1)",
    "deps": "echo node_modules symlink ready",
    "prespike": "bun run predeps",
    "spike": "bun run src/spike.ts"
  }
}
